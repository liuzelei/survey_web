<h1>Listing survey_suites</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @survey_suites.each do |survey_suite| %>
  <tr>
    <td><%= survey_suite.name %></td>
    <td><%= link_to 'Show', survey_suite %></td>
    <td><%= link_to 'Edit', edit_survey_suite_path(survey_suite) %></td>
    <td><%= link_to 'Destroy', survey_suite, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Survey suite', new_survey_suite_path %>
