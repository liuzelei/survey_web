<%= form_for(@survey_question_item) do |f| %>
  <% if @survey_question_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_question_item.errors.count, "error") %> prohibited this survey_question_item from being saved:</h2>

      <ul>
      <% @survey_question_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order %><br />
    <%= f.number_field :order %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
