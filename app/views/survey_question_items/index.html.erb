<h1>Listing survey_question_items</h1>

<table>
  <tr>
    <th>Order</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @survey_question_items.each do |survey_question_item| %>
  <tr>
    <td><%= survey_question_item.order %></td>
    <td><%= survey_question_item.body %></td>
    <td><%= link_to 'Show', survey_question_item %></td>
    <td><%= link_to 'Edit', edit_survey_question_item_path(survey_question_item) %></td>
    <td><%= link_to 'Destroy', survey_question_item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Survey question item', new_survey_question_item_path %>
